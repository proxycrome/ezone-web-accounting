import Home from './AccSetup';

const DashboardConfig = {
  settings: {
    layout: {
      config: {},
    },
  },
  routes: [
    {
      path: '/home',
      component: Home,
    },
  ],
};

export default DashboardConfig;
